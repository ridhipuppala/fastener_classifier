The data is spread in all the folders.

There are several codes for pre processing, like cropping the images, Converting to grayscale, conversion to numpy array and one hot vectors.
You can go through those codes to assess our work.
Testing is main code which trains and runs. The result data is also stored in results folder Performace comparosion data can be found in performance folder

This codes can only be run in linux with Tensorflow and Python3 installed
